{% extends 'AvroSupportBundle::layout.html.twig' %}
{% block content %}
    <div class="page-header">
        <h1>
            Answer List
        
            <a href="{{ path('avro_support_answer_new') }}" class="btn btn-primary btn-large pull-right">+ New Answer</a>
        </h1>
    </div>
    <div class="row">
        {% include 'AvroSupportBundle:Answer:table.html.twig' %}
    </div>
{% endblock %}

